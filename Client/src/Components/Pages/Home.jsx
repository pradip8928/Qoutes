import React from 'react'

const Home = () => {
    // / localstorage
    // useEffect(() => {
    //     const userInfo=localStorage.getItem("userInfo" );
    //     if (userInfo) {
    //     history.push("/");
    //     }
    // }, [history]);
  return (
    <div>
 
    </div>
  )
}

export default Home
